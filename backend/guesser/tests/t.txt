- test that only writes are allowed to pogotracker db
- ensure the mapping between dailypokemon -> pokemon is correct based on storing pokemon id's as pokemon in daily pokemon
- test that cron jobs run at midnight to select a new random pokemon
- test the endpoint that retrieves the pokemon to use on guesser page
- test the endpoint that receives the user's guess and checks against the daily pokemon
- test rate limiter (ip + user agent) where user is limited to 3 guesses per day
- each subsequent guess gets a new hint?


- test that in the event the cron job FAILS, the 'get todays pokemon' endpoint will still ATTEMPT to load one
- test a race condition where if it does FAIL two or more requests hitting the guess endpoint will only ever create one database record for today's date